{{#if users.length}}
    {{#each users}}
    <div>
        <p>Nombre: {{firstname}}</p>
        <p>Apellido: {{lastname}}</p>
    <br>
    </div>
    {{/each}}
{{else}}
    <p>No existen usuarios!</p>
{{/if}}